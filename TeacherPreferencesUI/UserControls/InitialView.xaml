<UserControl x:Class="TeacherPreferencesUI.UserControls.InitialView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TeacherPreferencesUI.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Viewbox>
            <TextBlock TextAlignment="Center" Margin="100">
                    Чтобы начать, выберите преподавателя
                    <LineBreak/>
                    или
                    <LineBreak/>
                    <Button Style="{StaticResource RoundedButton}" Content="откройте файл" Command="{Binding OpenFileCommand}"/>
            </TextBlock>
        </Viewbox>
    </Grid>
</UserControl>
